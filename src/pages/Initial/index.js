import ScreenContent from "../../components/screenContent";


function InitialScreen() {
    return (
        <ScreenContent ImageType={'search-logo'} text={'Start with searching a GitHub user'} textStyle={'max-w-[210px]'} />
    );
}
export default InitialScreen;
